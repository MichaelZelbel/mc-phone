[Unit]
Description=mc-phone RPC (one connection)

[Service]
Type=simple
EnvironmentFile=/etc/mc-phone/config.env
ExecStart=/usr/bin/python3 -I /opt/mc-phone/service.py
StandardInput=socket
StandardOutput=socket
# Never the socket: the reply is exactly one JSON object on stdout, and a Python warning
# sharing that stream would corrupt the reply the caller parses.
StandardError=journal
UMask=0077
NoNewPrivileges=true
